---
/**
 * BINSIGHT — ADMIN LAYOUT
 * --------------------------------------------------------------------------
 * Internal administration workspace for BINSIGHT.
 *
 * Architecture:
 * - Global Navbar
 * - Persistent desktop sidebar
 * - Independent desktop sidebar scrolling
 * - Independent main-content scrolling
 * - Mobile sidebar drawer
 * - Page-owned main content
 * - Independent footer
 *
 * IMPORTANT:
 * - No page-level admin header is rendered.
 * - Pages own their own visual hierarchy.
 * - Main content has no black wrapper/border.
 * - Footer is structurally separated from workspace.
 * - Layout must never create horizontal page overflow.
 * - Desktop sidebar and main content scroll independently.
 *
 * Security note:
 * - This layout is UI infrastructure only.
 * - It does NOT replace authentication / authorization.
 * - Access control belongs to src/lib/auth/*
 * - Firebase Security Rules remain authoritative.
 * --------------------------------------------------------------------------
 */

import {
  Activity,
  Building2,
  ChartColumn,
  CircleCheck,
  LayoutDashboard,
  LockKeyhole,
  MapPin,
  Menu,
  Newspaper,
  Recycle,
  ShieldCheck,
  TriangleAlert,
  Users,
  X,
} from "@lucide/astro";

import Navbar from "@components/layout/Navbar.astro";
import Footer from "@components/layout/Footer.astro";

type AdminSection =
  | "overview"
  | "organizations"
  | "locations"
  | "waste"
  | "reports"
  | "users"
  | "content";

interface Breadcrumb {
  label: string;
  href?: string;
}

interface Props {
  title?: string;
  description?: string;
  breadcrumbs?: Breadcrumb[];
  activeSection?: AdminSection;
  adminName?: string;
  adminInitials?: string;
  bodyClass?: string;
  noindex?: boolean;
}

const {
  title = "Admin Dashboard",
  description =
    "Kelola data, verifikasi, konten, dan aktivitas platform BINSIGHT.",
  breadcrumbs = [
    {
      label: "Admin",
      href: "/admin",
    },
    {
      label: "Dashboard",
    },
  ],
  activeSection = "overview",
  adminName = "Administrator",
  adminInitials = "AD",
  bodyClass = "",
  noindex = true,
} = Astro.props;

const siteUrl =
  import.meta.env.PUBLIC_SITE_URL ||
  Astro.site?.toString() ||
  "https://binsight.id";

const appName =
  import.meta.env.PUBLIC_APP_NAME ||
  "BINSIGHT";

const absoluteUrl = (
  value: string,
): string => {
  if (
    value.startsWith("http://") ||
    value.startsWith("https://")
  ) {
    return value;
  }

  return new URL(
    value.startsWith("/")
      ? value
      : `/${value}`,
    siteUrl,
  ).toString();
};

const canonicalUrl = absoluteUrl(
  Astro.url.pathname,
);

const fullTitle =
  title.startsWith("BINSIGHT")
    ? title
    : `${title} — BINSIGHT`;

const robotsContent = noindex
  ? "noindex, nofollow"
  : "index, follow";

const navItems: Array<{
  id: AdminSection;
  label: string;
  description: string;
  href: string;
  icon: typeof LayoutDashboard;
}> = [
  {
    id: "overview",
    label: "Ringkasan",
    description: "Overview platform",
    href: "/admin",
    icon: LayoutDashboard,
  },
  {
    id: "organizations",
    label: "Organisasi",
    description: "Verifikasi & kelola",
    href: "/admin/organizations",
    icon: Building2,
  },
  {
    id: "locations",
    label: "Lokasi",
    description: "Wilayah & titik",
    href: "/admin/locations",
    icon: MapPin,
  },
  {
    id: "waste",
    label: "Sampah",
    description: "Kategori & data",
    href: "/admin/waste",
    icon: Recycle,
  },
  {
    id: "reports",
    label: "Laporan",
    description: "Tinjau & tindak lanjut",
    href: "/admin/reports",
    icon: TriangleAlert,
  },
  {
    id: "users",
    label: "Pengguna",
    description: "Akun & akses",
    href: "/admin/users",
    icon: Users,
  },
  {
    id: "content",
    label: "Konten",
    description: "Edukasi & informasi",
    href: "/admin/content",
    icon: Newspaper,
  },
];

const managementLinks = [
  {
    label: "Dashboard Publik",
    href: "/dashboard",
    icon: ChartColumn,
  },
  {
    label: "Lihat Peta",
    href: "/map",
    icon: MapPin,
  },
];

const isActive = (
  section: AdminSection,
): boolean => {
  return activeSection === section;
};
---

<!doctype html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />

    <meta
      name="viewport"
      content="width=device-width, initial-scale=1"
    />

    <meta
      name="theme-color"
      content="#090A08"
    />

    <meta
      name="color-scheme"
      content="light dark"
    />

    <meta
      name="application-name"
      content={appName}
    />

    <meta
      name="robots"
      content={robotsContent}
    />

    <meta
      name="googlebot"
      content={robotsContent}
    />

    <title>{fullTitle}</title>

    <meta
      name="description"
      content={description}
    />

    <link
      rel="canonical"
      href={canonicalUrl}
    />

    <link
      rel="icon"
      href="/favicon.svg"
      type="image/svg+xml"
    />

    <meta
      property="og:type"
      content="website"
    />

    <meta
      property="og:site_name"
      content="BINSIGHT"
    />

    <meta
      property="og:locale"
      content="id_ID"
    />

    <meta
      property="og:title"
      content={fullTitle}
    />

    <meta
      property="og:description"
      content={description}
    />

    <meta
      property="og:url"
      content={canonicalUrl}
    />

    <meta
      property="og:image:alt"
      content={fullTitle}
    />

    <meta
      name="twitter:card"
      content="summary"
    />

    <meta
      name="twitter:title"
      content={fullTitle}
    />

    <meta
      name="twitter:description"
      content={description}
    />

    <script
      is:inline
      type="application/ld+json"
      set:html={JSON.stringify({
        "@context": "https://schema.org",
        "@type": "WebPage",
        name: title,
        url: canonicalUrl,
        description,
        inLanguage: "id-ID",
        isPartOf: {
          "@type": "WebSite",
          name: "BINSIGHT",
          url: siteUrl,
        },
      })}
    />

    <slot name="head" />
  </head>

  <body
    class:list={[
      "admin-layout",
      bodyClass,
    ]}
  >
    <a
      class="admin-layout__skip-link"
      href="#admin-main-content"
    >
      Lewati ke konten admin
    </a>

    <div class="admin-layout__shell">
      <Navbar />

      <!-- ============================================================
           WORKSPACE
           Sidebar + main content have independent vertical scrolling.
      ============================================================= -->
      <div class="admin-layout__workspace">
        <!-- ============================================================
             SIDEBAR
        ============================================================= -->
        <aside
          id="admin-sidebar"
          class="admin-layout__sidebar"
          data-admin-sidebar
          aria-label="Navigasi administrasi"
        >
          <div class="admin-layout__sidebar-inner">
            <div class="admin-layout__system-heading">
              <div class="admin-layout__system-title">
                <span class="admin-layout__system-icon">
                  <ShieldCheck
                    size={17}
                    stroke-width={2}
                    aria-hidden="true"
                  />
                </span>

                <span>
                  Admin BINSIGHT
                </span>
              </div>

              <span class="admin-layout__system-badge">
                INTERNAL
              </span>
            </div>

            <div class="admin-layout__admin-card">
              <div
                class="admin-layout__admin-avatar"
                aria-hidden="true"
              >
                {adminInitials}
              </div>

              <div class="admin-layout__admin-copy">
                <strong>
                  {adminName}
                </strong>

                <span>
                  <LockKeyhole
                    size={12}
                    stroke-width={2}
                    aria-hidden="true"
                  />

                  Administrator
                </span>
              </div>
            </div>

            <nav
              class="admin-layout__nav"
              aria-label="Menu administrasi"
            >
              <p class="admin-layout__nav-label">
                Manajemen platform
              </p>

              {
                navItems.map((item) => {
                  const active =
                    isActive(item.id);

                  return (
                    <a
                      href={item.href}
                      class:list={[
                        "admin-layout__nav-item",
                        {
                          "admin-layout__nav-item--active":
                            active,
                        },
                      ]}
                      aria-current={
                        active
                          ? "page"
                          : undefined
                      }
                    >
                      <span class="admin-layout__nav-icon">
                        <item.icon
                          size={17}
                          stroke-width={1.9}
                          aria-hidden="true"
                        />
                      </span>

                      <span class="admin-layout__nav-copy">
                        <strong>
                          {item.label}
                        </strong>

                        <small>
                          {item.description}
                        </small>
                      </span>
                    </a>
                  );
                })
              }
            </nav>

            <div class="admin-layout__divider"></div>

            <div class="admin-layout__system-status">
              <div class="admin-layout__system-status-header">
                <span class="admin-layout__system-status-icon">
                  <Activity
                    size={14}
                    stroke-width={2}
                    aria-hidden="true"
                  />
                </span>

                <span>
                  Status platform
                </span>
              </div>

              <div class="admin-layout__status-row">
                <span class="admin-layout__status-dot"></span>

                <span>
                  Operasional
                </span>
              </div>

              <p>
                Status yang ditampilkan di sini bersifat
                informatif. Validasi akses tetap dilakukan
                oleh sistem autentikasi dan Firebase Rules.
              </p>
            </div>

            <div class="admin-layout__divider"></div>

            <nav
              class="admin-layout__secondary-nav"
              aria-label="Akses cepat"
            >
              <p class="admin-layout__nav-label">
                Akses cepat
              </p>

              {
                managementLinks.map(
                  (item) => (
                    <a
                      href={item.href}
                      class="admin-layout__secondary-link"
                    >
                      <span>
                        <item.icon
                          size={15}
                          stroke-width={1.9}
                          aria-hidden="true"
                        />
                      </span>

                      {item.label}
                    </a>
                  ),
                )
              }
            </nav>

            <div class="admin-layout__security-note">
              <span class="admin-layout__security-icon">
                <LockKeyhole
                  size={14}
                  stroke-width={2}
                  aria-hidden="true"
                />
              </span>

              <div>
                <strong>
                  Area terbatas
                </strong>

                <p>
                  Halaman admin hanya untuk pengguna
                  dengan hak akses yang sesuai.
                </p>
              </div>
            </div>
          </div>
        </aside>

        <!-- ============================================================
             CONTENT WORKSPACE
        ============================================================= -->
        <div class="admin-layout__content">
          <!--
            IMPORTANT:
            Tidak menggunakan <header>.
            Ini hanya mobile navigation utility.
          -->
          <div class="admin-layout__mobile-bar">
            <button
              type="button"
              class="admin-layout__mobile-toggle"
              data-admin-sidebar-toggle
              aria-controls="admin-sidebar"
              aria-expanded="false"
              aria-label="Buka navigasi admin"
            >
              <Menu
                class="admin-layout__mobile-menu-icon"
                size={20}
                stroke-width={2}
                aria-hidden="true"
              />

              <X
                class="admin-layout__mobile-close-icon"
                size={20}
                stroke-width={2}
                aria-hidden="true"
              />
            </button>

            <div class="admin-layout__mobile-context">
              <span>
                Admin BINSIGHT
              </span>

              <strong>
                {title}
              </strong>
            </div>

            <div
              class="admin-layout__mobile-status"
              aria-hidden="true"
            >
              <CircleCheck
                size={17}
                stroke-width={2}
              />
            </div>
          </div>

          <!-- ==========================================================
               PAGE-OWNED CONTENT
          =========================================================== -->
          <main
            id="admin-main-content"
            class="admin-layout__main"
            tabindex="-1"
          >
            <div class="admin-layout__main-container">
              <slot />
            </div>
          </main>
        </div>
      </div>

      <!-- ============================================================
           INDEPENDENT FOOTER
      ============================================================= -->
      <div class="admin-layout__footer">
        <Footer />
      </div>
    </div>

    <div
      class="admin-layout__backdrop"
      data-admin-backdrop
      aria-hidden="true"
    ></div>

    <script>
      const adminRoot =
        document.querySelector(
          ".admin-layout",
        );

      if (adminRoot) {
        const sidebar =
          adminRoot.querySelector(
            "[data-admin-sidebar]",
          );

        const toggle =
          adminRoot.querySelector(
            "[data-admin-sidebar-toggle]",
          );

        const backdrop =
          adminRoot.querySelector(
            "[data-admin-backdrop]",
          );

        const openSidebar = () => {
          if (
            !(sidebar instanceof HTMLElement) ||
            !(toggle instanceof HTMLButtonElement)
          ) {
            return;
          }

          sidebar.classList.add(
            "admin-layout__sidebar--open",
          );

          adminRoot.classList.add(
            "admin-layout--sidebar-open",
          );

          toggle.setAttribute(
            "aria-expanded",
            "true",
          );

          toggle.setAttribute(
            "aria-label",
            "Tutup navigasi admin",
          );

          document.documentElement.classList.add(
            "admin-scroll-locked",
          );

          document.body.classList.add(
            "admin-scroll-locked",
          );
        };

        const closeSidebar = () => {
          if (
            !(sidebar instanceof HTMLElement) ||
            !(toggle instanceof HTMLButtonElement)
          ) {
            return;
          }

          sidebar.classList.remove(
            "admin-layout__sidebar--open",
          );

          adminRoot.classList.remove(
            "admin-layout--sidebar-open",
          );

          toggle.setAttribute(
            "aria-expanded",
            "false",
          );

          toggle.setAttribute(
            "aria-label",
            "Buka navigasi admin",
          );

          document.documentElement.classList.remove(
            "admin-scroll-locked",
          );

          document.body.classList.remove(
            "admin-scroll-locked",
          );
        };

        if (
          toggle instanceof HTMLButtonElement
        ) {
          toggle.addEventListener(
            "click",
            () => {
              const isOpen =
                sidebar instanceof HTMLElement &&
                sidebar.classList.contains(
                  "admin-layout__sidebar--open",
                );

              if (isOpen) {
                closeSidebar();
              } else {
                openSidebar();
              }
            },
          );
        }

        if (
          backdrop instanceof HTMLElement
        ) {
          backdrop.addEventListener(
            "click",
            closeSidebar,
          );
        }

        if (
          sidebar instanceof HTMLElement
        ) {
          sidebar
            .querySelectorAll("a")
            .forEach((link) => {
              link.addEventListener(
                "click",
                closeSidebar,
              );
            });
        }

        document.addEventListener(
          "keydown",
          (event) => {
            if (event.key === "Escape") {
              closeSidebar();
            }
          },
        );

        window.addEventListener(
          "resize",
          () => {
            if (window.innerWidth > 1000) {
              closeSidebar();
            }
          },
        );
      }
    </script>
  </body>
</html>

<style>
  /* ================================================================
     GLOBAL BOX MODEL / OVERFLOW SAFETY
  ================================================================= */

  :global(*) {
    box-sizing: border-box;
  }

  :global(html) {
    width: 100%;
    max-width: 100%;

    margin: 0;
    padding: 0;

    overflow-x: hidden;
  }

  :global(body) {
    width: 100%;
    max-width: 100%;

    margin: 0;
    padding: 0;

    overflow-x: hidden;
  }

  /* ================================================================
     ROOT
  ================================================================= */

  .admin-layout {
    --acid: #dfff00;
    --acid-soft: #efff70;
    --ink: #090a08;
    --ink-2: #11130f;
    --ink-3: #1b1f18;
    --paper: #f2f0e6;
    --paper-2: #e4e1d4;
    --paper-3: #cfcabd;
    --white: #fffef8;
    --muted: #6c7068;
    --orange: #ff6a00;
    --green: #87b800;

    width: 100%;
    max-width: 100%;
    min-width: 0;

    min-height: 100vh;

    margin: 0;

    overflow-x: hidden;

    color: var(--ink);
    background: var(--paper);
  }

  .admin-layout__shell {
    width: 100%;
    max-width: 100%;
    min-width: 0;

    min-height: 100vh;

    display: flex;
    flex-direction: column;

    overflow-x: hidden;

    background: var(--paper);
  }

  /* ================================================================
     WORKSPACE
  ================================================================= */

  .admin-layout__workspace {
    width: 100%;
    max-width: 100%;
    min-width: 0;

    /*
     * The workspace itself is a fixed-height viewport region.
     * Sidebar and content scroll inside it independently.
     */
    height: calc(100vh - 76px);
    min-height: 0;

    display: flex;
    align-items: stretch;

    overflow: hidden;

    background: var(--paper);
  }

  /* ================================================================
     SIDEBAR — DESKTOP INDEPENDENT SCROLL
  ================================================================= */

  .admin-layout__sidebar {
    position: relative;

    flex:
      0 0
      292px;

    width: 292px;
    max-width: 292px;
    min-width: 292px;

    height: 100%;
    min-height: 0;

    /*
     * Sidebar owns its own vertical scroll.
     */
    overflow-x: hidden;
    overflow-y: auto;

    overscroll-behavior: contain;

    border-right: 2px solid var(--ink);

    background:
      linear-gradient(
        180deg,
        var(--paper) 0%,
        var(--paper-2) 100%
      );

    color: var(--ink);

    scrollbar-width: thin;
    scrollbar-color:
      #aaaD9f
      transparent;
  }

  .admin-layout__sidebar::-webkit-scrollbar {
    width: 7px;
  }

  .admin-layout__sidebar::-webkit-scrollbar-track {
    background: transparent;
  }

  .admin-layout__sidebar::-webkit-scrollbar-thumb {
    background: #aaaD9f;
  }

  .admin-layout__sidebar-inner {
    width: 100%;
    max-width: 100%;
    min-width: 0;

    min-height: 100%;

    display: flex;
    flex-direction: column;

    padding: 20px 14px 22px;

    overflow-x: hidden;
  }

  .admin-layout__system-heading {
    display: flex;
    align-items: center;
    justify-content: space-between;

    gap: 10px;

    min-width: 0;

    padding: 3px 8px 15px;

    border-bottom: 2px solid var(--ink);
  }

  .admin-layout__system-title {
    min-width: 0;

    display: flex;
    align-items: center;

    gap: 9px;

    color: var(--ink);

    font-family:
      Sora,
      Inter,
      system-ui,
      sans-serif;

    font-size: 0.7rem;
    font-weight: 800;

    letter-spacing: -0.015em;

    text-transform: uppercase;
  }

  .admin-layout__system-title > span:last-child {
    min-width: 0;

    overflow: hidden;

    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .admin-layout__system-icon {
    width: 30px;
    height: 30px;

    display: grid;
    place-items: center;

    flex-shrink: 0;

    border: 2px solid var(--ink);

    border-radius: 0;

    color: var(--acid);
    background: var(--ink);

    box-shadow:
      3px 3px 0 var(--orange);
  }

  .admin-layout__system-badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;

    flex-shrink: 0;

    min-height: 22px;

    padding: 0 8px;

    border: 1px solid var(--ink);
    border-radius: 0;

    color: var(--ink);
    background: var(--acid);

    font-family:
      "DM Mono",
      "SFMono-Regular",
      Consolas,
      monospace;

    font-size: 0.48rem;
    font-weight: 700;

    line-height: 1;

    letter-spacing: 0.08em;

    text-transform: uppercase;
  }

  /* ================================================================
     ADMIN IDENTITY
  ================================================================= */

  .admin-layout__admin-card {
    min-width: 0;
    max-width: 100%;

    display: flex;
    align-items: center;

    gap: 11px;

    margin: 16px 2px 21px;

    padding: 12px;

    border: 2px solid var(--ink);

    border-radius: 0;

    background: var(--white);

    box-shadow:
      4px 4px 0 var(--acid);
  }

  .admin-layout__admin-avatar {
    width: 42px;
    height: 42px;

    display: grid;
    place-items: center;

    flex-shrink: 0;

    border: 2px solid var(--ink);

    border-radius: 0;

    color: var(--acid);
    background: var(--ink);

    font-family:
      Sora,
      Inter,
      system-ui,
      sans-serif;

    font-size: 0.68rem;
    font-weight: 800;

    letter-spacing: 0.04em;
  }

  .admin-layout__admin-copy {
    min-width: 0;

    display: flex;
    flex-direction: column;

    gap: 4px;
  }

  .admin-layout__admin-copy strong {
    min-width: 0;

    overflow: hidden;

    color: var(--ink);

    font-family:
      Sora,
      Inter,
      system-ui,
      sans-serif;

    font-size: 0.7rem;
    font-weight: 800;

    line-height: 1.25;

    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .admin-layout__admin-copy span {
    min-width: 0;

    display: inline-flex;
    align-items: center;

    gap: 5px;

    color: var(--muted);

    font-family:
      "DM Mono",
      "SFMono-Regular",
      Consolas,
      monospace;

    font-size: 0.49rem;
    font-weight: 700;

    line-height: 1.2;

    letter-spacing: 0.04em;

    text-transform: uppercase;
  }

  /* ================================================================
     NAVIGATION
  ================================================================= */

  .admin-layout__nav {
    min-width: 0;

    display: grid;
    gap: 5px;
  }

  .admin-layout__nav-label {
    min-width: 0;

    margin: 0 9px 8px;

    color: var(--orange);

    font-family:
      "DM Mono",
      "SFMono-Regular",
      Consolas,
      monospace;

    font-size: 0.51rem;
    font-weight: 700;

    line-height: 1;

    letter-spacing: 0.1em;

    text-transform: uppercase;
  }

  .admin-layout__nav-item {
    position: relative;

    min-width: 0;
    max-width: 100%;

    display: flex;
    align-items: center;

    gap: 10px;

    min-height: 54px;

    padding: 7px 9px;

    border: 2px solid transparent;

    border-radius: 0;

    color: #5f645c;

    background: transparent;

    text-decoration: none;

    overflow: hidden;

    transition:
      color 150ms ease,
      background 150ms ease,
      border-color 150ms ease,
      transform 150ms ease;
  }

  .admin-layout__nav-item:hover {
    color: var(--ink);

    border-color: #bfc1b6;

    background: var(--white);

    transform: translateX(2px);
  }

  .admin-layout__nav-item--active {
    color: var(--ink);

    border-color: var(--ink);

    background: var(--acid);

    box-shadow:
      4px 4px 0 var(--ink);
  }

  .admin-layout__nav-item--active::before {
    content: "";

    position: absolute;

    top: -2px;
    bottom: -2px;

    left: -16px;

    width: 4px;

    background: var(--orange);
  }

  .admin-layout__nav-icon {
    width: 36px;
    height: 36px;

    display: grid;
    place-items: center;

    flex-shrink: 0;

    border: 1px solid #c6c8bc;

    border-radius: 0;

    color: #5f645c;

    background: #e7e5da;

    transition:
      background 150ms ease,
      color 150ms ease,
      border-color 150ms ease;
  }

  .admin-layout__nav-item:hover
    .admin-layout__nav-icon {
    color: var(--ink);

    border-color: var(--ink);

    background: var(--paper);
  }

  .admin-layout__nav-item--active
    .admin-layout__nav-icon {
    color: var(--acid);

    border-color: var(--ink);

    background: var(--ink);
  }

  .admin-layout__nav-copy {
    min-width: 0;
    max-width: 100%;

    display: flex;
    flex-direction: column;

    gap: 3px;

    overflow: hidden;
  }

  .admin-layout__nav-copy strong {
    min-width: 0;

    overflow: hidden;

    color: currentColor;

    font-family:
      Sora,
      Inter,
      system-ui,
      sans-serif;

    font-size: 0.68rem;
    font-weight: 800;

    line-height: 1.2;

    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .admin-layout__nav-copy small {
    min-width: 0;

    overflow: hidden;

    color: #797d74;

    font-family:
      Inter,
      system-ui,
      sans-serif;

    font-size: 0.57rem;
    font-weight: 500;

    line-height: 1.3;

    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .admin-layout__nav-item--active
    .admin-layout__nav-copy
    small {
    color: #4f542f;
  }

  /* ================================================================
     DIVIDER
  ================================================================= */

  .admin-layout__divider {
    width: calc(100% - 8px);
    max-width: calc(100% - 8px);

    height: 2px;

    margin: 18px 4px;

    background: var(--ink);
  }

  /* ================================================================
     SYSTEM STATUS
  ================================================================= */

  .admin-layout__system-status {
    min-width: 0;
    max-width: 100%;

    padding: 13px;

    border: 2px solid var(--ink);

    border-radius: 0;

    background: var(--white);

    overflow: hidden;
  }

  .admin-layout__system-status-header {
    min-width: 0;

    display: flex;
    align-items: center;

    gap: 7px;

    color: var(--ink);

    font-family:
      "DM Mono",
      "SFMono-Regular",
      Consolas,
      monospace;

    font-size: 0.5rem;
    font-weight: 700;

    letter-spacing: 0.08em;

    text-transform: uppercase;
  }

  .admin-layout__system-status-header > span:last-child {
    min-width: 0;

    overflow: hidden;

    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .admin-layout__system-status-icon {
    width: 25px;
    height: 25px;

    display: grid;
    place-items: center;

    flex-shrink: 0;

    border: 1px solid var(--ink);

    border-radius: 0;

    color: var(--ink);

    background: var(--acid);
  }

  .admin-layout__status-row {
    display: flex;
    align-items: center;

    gap: 7px;

    margin-top: 11px;

    color: var(--ink);

    font-size: 0.62rem;
    font-weight: 800;
  }

  .admin-layout__status-dot {
    width: 7px;
    height: 7px;

    flex-shrink: 0;

    background: var(--green);

    box-shadow:
      0 0 0 3px rgb(135 184 0 / 15%);
  }

  .admin-layout__system-status p {
    margin: 9px 0 0;

    color: var(--muted);

    font-size: 0.57rem;

    line-height: 1.55;

    overflow-wrap: anywhere;
  }

  /* ================================================================
     SECONDARY NAV
  ================================================================= */

  .admin-layout__secondary-nav {
    min-width: 0;

    display: grid;
    gap: 4px;
  }

  .admin-layout__secondary-link {
    min-width: 0;
    max-width: 100%;

    display: flex;
    align-items: center;

    gap: 9px;

    min-height: 38px;

    padding: 7px 9px;

    border: 1px solid transparent;

    border-radius: 0;

    color: #5f645c;

    font-size: 0.62rem;
    font-weight: 750;

    text-decoration: none;

    overflow: hidden;

    transition:
      color 150ms ease,
      background 150ms ease,
      border-color 150ms ease;
  }

  .admin-layout__secondary-link > span {
    width: 28px;
    height: 28px;

    display: grid;
    place-items: center;

    flex-shrink: 0;

    border: 1px solid #c6c8bc;

    border-radius: 0;

    color: var(--muted);

    background: #e7e5da;
  }

  .admin-layout__secondary-link:hover {
    color: var(--ink);

    border-color: #bfc1b6;

    background: var(--white);
  }

  .admin-layout__secondary-link:hover > span {
    color: var(--ink);

    border-color: var(--ink);

    background: var(--acid);
  }

  /* ================================================================
     SECURITY NOTE
  ================================================================= */

  .admin-layout__security-note {
    min-width: 0;
    max-width: 100%;

    display: flex;
    align-items: flex-start;

    gap: 9px;

    margin-top: auto;

    padding: 12px;

    border:
      1px solid #c9cbbf;

    border-left:
      3px solid var(--orange);

    border-radius: 0;

    background: var(--paper-2);

    overflow: hidden;
  }

  .admin-layout__security-icon {
    width: 28px;
    height: 28px;

    display: grid;
    place-items: center;

    flex-shrink: 0;

    border: 1px solid var(--ink);

    border-radius: 0;

    color: var(--acid);

    background: var(--ink);
  }

  .admin-layout__security-note > div {
    min-width: 0;
  }

  .admin-layout__security-note strong {
    display: block;

    color: var(--ink);

    font-size: 0.6rem;
    font-weight: 800;

    text-transform: uppercase;
  }

  .admin-layout__security-note p {
    margin: 4px 0 0;

    color: var(--muted);

    font-size: 0.55rem;

    line-height: 1.55;

    overflow-wrap: anywhere;
  }

  /* ================================================================
     MAIN CONTENT — INDEPENDENT SCROLL
  ================================================================= */

  .admin-layout__content {
    flex:
      1 1 0;

    width: 0;
    max-width: calc(100% - 292px);

    min-width: 0;
    min-height: 0;

    display: flex;
    flex-direction: column;

    /*
     * Main content owns its vertical scrollbar.
     */
    overflow-x: hidden;
    overflow-y: auto;

    overscroll-behavior: contain;

    background: var(--paper);

    scrollbar-width: thin;
    scrollbar-color:
      #aaaD9f
      transparent;
  }

  .admin-layout__content::-webkit-scrollbar {
    width: 8px;
  }

  .admin-layout__content::-webkit-scrollbar-track {
    background: transparent;
  }

  .admin-layout__content::-webkit-scrollbar-thumb {
    background: #aaaD9f;
  }

  /*
   * NO ADMIN HEADER.
   */

  .admin-layout__mobile-bar {
    display: none;
  }

  .admin-layout__main {
    flex:
      1 0 auto;

    width: 100%;
    max-width: 100%;

    min-width: 0;

    outline: none;

    overflow-x: hidden;

    background: var(--paper);
  }

  .admin-layout__main:focus {
    outline: none;
  }

  .admin-layout__main-container {
    width: 100%;
    max-width: 100%;

    min-width: 0;

    margin: 0;

    padding: 0;

    overflow-x: hidden;

    background: transparent;
  }

  .admin-layout__main-container > * {
    min-width: 0;
    max-width: 100%;
  }

  .admin-layout__main-container img,
  .admin-layout__main-container svg,
  .admin-layout__main-container video,
  .admin-layout__main-container canvas,
  .admin-layout__main-container iframe {
    max-width: 100%;
  }

  .admin-layout__main-container
    :where(
      p,
      h1,
      h2,
      h3,
      h4,
      h5,
      h6,
      span,
      strong,
      small,
      a,
      li,
      td,
      th
    ) {
    overflow-wrap: anywhere;
  }

  .admin-layout__main-container table {
    width: 100%;
    max-width: 100%;
  }

  .admin-layout__main-container
    :where(
      pre,
      .table-wrapper,
      [data-table-scroll]
    ) {
    max-width: 100%;
    overflow-x: auto;
  }

  .admin-layout__main-container
    :where(
      [class*="grid"],
      [class*="row"],
      [class*="flex"]
    ) {
    min-width: 0;
    max-width: 100%;
  }

  .admin-layout__main-container
    :where(
      [class*="grid"] > *,
      [class*="row"] > *,
      [class*="flex"] > *
    ) {
    min-width: 0;
    max-width: 100%;
  }

  /* ================================================================
     FOOTER
  ================================================================= */

  .admin-layout__footer {
    flex:
      0 0 auto;

    width: 100%;
    max-width: 100%;
    min-width: 0;

    overflow-x: hidden;

    border-top: 2px solid var(--ink);

    background: var(--ink);

    color: var(--white);
  }

  /* ================================================================
     BACKDROP
  ================================================================= */

  .admin-layout__backdrop {
    display: none;
  }

  /* ================================================================
     SKIP LINK
  ================================================================= */

  .admin-layout__skip-link {
    position: fixed;

    top: 12px;
    left: 12px;

    z-index: 2400;

    display: inline-flex;
    align-items: center;
    justify-content: center;

    min-height: 42px;

    max-width: calc(100vw - 24px);

    padding: 0 15px;

    border: 2px solid var(--ink);

    border-radius: 0;

    color: var(--acid);

    background: var(--ink);

    box-shadow:
      5px 5px 0 var(--orange);

    font-family:
      Inter,
      system-ui,
      sans-serif;

    font-size: 0.72rem;
    font-weight: 800;

    text-decoration: none;

    opacity: 0;
    pointer-events: none;

    transform: translateY(-12px);

    transition:
      opacity 140ms ease,
      transform 140ms ease;
  }

  .admin-layout__skip-link:focus {
    opacity: 1;

    pointer-events: auto;

    transform: translateY(0);
  }

  /* ================================================================
     MOBILE
  ================================================================= */

  @media (max-width: 1000px) {
    .admin-layout__workspace {
      display: block;

      width: 100%;
      max-width: 100%;
      min-width: 0;

      height: auto;
      min-height: 0;

      overflow: visible;
    }

    /*
     * Mobile sidebar becomes a drawer.
     */
    .admin-layout__sidebar {
      position: fixed;

      top: 76px;
      left: 0;

      z-index: 1100;

      flex: none;

      width:
        min(
          318px,
          calc(100vw - 34px)
        );

      max-width:
        min(
          318px,
          calc(100vw - 34px)
        );

      min-width: 0;

      height:
        calc(
          100vh - 76px
        );

      min-height: 0;

      overflow-x: hidden;
      overflow-y: auto;

      border-right:
        2px solid var(--ink);

      box-shadow:
        8px 0 0 var(--acid);

      background:
        linear-gradient(
          180deg,
          var(--paper) 0%,
          var(--paper-2) 100%
        );

      transform:
        translateX(-105%);

      transition:
        transform 220ms ease;
    }

    .admin-layout__sidebar--open {
      transform: translateX(0);
    }

    .admin-layout__sidebar-inner {
      min-height: 100%;

      overflow-x: hidden;
    }

    /*
     * Mobile page returns to normal browser scrolling.
     */
    .admin-layout__content {
      width: 100%;
      max-width: 100%;

      min-width: 0;
      min-height: 0;

      display: flex;

      overflow-x: hidden;
      overflow-y: visible;

      overscroll-behavior: auto;
    }

    .admin-layout__mobile-bar {
      display: flex;
      align-items: center;

      gap: 10px;

      width: 100%;
      max-width: 100%;
      min-width: 0;

      min-height: 70px;

      padding:
        12px 18px;

      border-bottom:
        2px solid var(--ink);

      background:
        var(--white);

      overflow: hidden;
    }

    .admin-layout__mobile-toggle {
      width: 40px;
      height: 40px;

      display: grid;
      place-items: center;

      flex-shrink: 0;

      border:
        2px solid var(--ink);

      border-radius: 0;

      color: var(--acid);

      background:
        var(--ink);

      cursor: pointer;

      box-shadow:
        3px 3px 0 var(--orange);

      transition:
        transform 150ms ease,
        box-shadow 150ms ease;
    }

    .admin-layout__mobile-toggle:hover {
      color: var(--ink);

      background: var(--acid);

      transform:
        translate(-1px, -1px);

      box-shadow:
        4px 4px 0 var(--orange);
    }

    .admin-layout__mobile-toggle:focus-visible {
      outline:
        3px solid var(--orange);

      outline-offset:
        3px;
    }

    .admin-layout__mobile-menu-icon,
    .admin-layout__mobile-close-icon {
      grid-area: 1 / 1;

      transition:
        opacity 150ms ease,
        transform 150ms ease;
    }

    .admin-layout__mobile-close-icon {
      opacity: 0;

      transform:
        rotate(-30deg)
        scale(0.75);
    }

    .admin-layout--sidebar-open
      .admin-layout__mobile-menu-icon {
      opacity: 0;

      transform:
        rotate(30deg)
        scale(0.75);
    }

    .admin-layout--sidebar-open
      .admin-layout__mobile-close-icon {
      opacity: 1;

      transform:
        rotate(0)
        scale(1);
    }

    .admin-layout__mobile-context {
      min-width: 0;

      flex: 1 1 auto;

      display: flex;
      flex-direction: column;

      gap: 3px;

      overflow: hidden;
    }

    .admin-layout__mobile-context span {
      min-width: 0;

      overflow: hidden;

      color: var(--orange);

      font-family:
        "DM Mono",
        "SFMono-Regular",
        Consolas,
        monospace;

      font-size: 0.49rem;
      font-weight: 700;

      line-height: 1;

      letter-spacing: 0.09em;

      text-transform: uppercase;

      text-overflow: ellipsis;

      white-space: nowrap;
    }

    .admin-layout__mobile-context strong {
      min-width: 0;

      overflow: hidden;

      color: var(--ink);

      font-family:
        Sora,
        Inter,
        system-ui,
        sans-serif;

      font-size: 0.74rem;
      font-weight: 800;

      line-height: 1.25;

      text-overflow: ellipsis;

      white-space: nowrap;
    }

    .admin-layout__mobile-status {
      width: 40px;
      height: 40px;

      display: grid;
      place-items: center;

      flex-shrink: 0;

      border:
        2px solid var(--ink);

      border-radius: 0;

      color: var(--ink);

      background:
        var(--acid);
    }

    .admin-layout__main {
      width: 100%;
      max-width: 100%;

      min-width: 0;
    }

    .admin-layout__main-container {
      width: 100%;
      max-width: 100%;

      min-width: 0;

      overflow-x: hidden;
    }

    .admin-layout__backdrop {
      position: fixed;

      inset:
        76px 0 0;

      z-index: 1050;

      display: block;

      width: 100%;
      max-width: 100%;

      background:
        rgb(
          9 10 8 / 58%
        );

      opacity: 0;

      visibility: hidden;

      pointer-events: none;

      transition:
        opacity 180ms ease,
        visibility 180ms ease;
    }

    .admin-layout--sidebar-open
      .admin-layout__backdrop {
      opacity: 1;

      visibility: visible;

      pointer-events: auto;
    }
  }

  /* ================================================================
     SMALL MOBILE
  ================================================================= */

  @media (max-width: 560px) {
    .admin-layout__sidebar {
      top: 70px;

      width:
        min(
          300px,
          calc(100vw - 24px)
        );

      max-width:
        min(
          300px,
          calc(100vw - 24px)
        );

      height:
        calc(
          100vh - 70px
        );
    }

    .admin-layout__backdrop {
      inset:
        70px 0 0;
    }

    .admin-layout__mobile-bar {
      min-height: 64px;

      padding:
        10px 12px;
    }

    .admin-layout__main-container {
      width: 100%;
      max-width: 100%;

      overflow-x: hidden;
    }
  }

  /* ================================================================
     FOCUS
  ================================================================= */

  .admin-layout__nav-item:focus-visible,
  .admin-layout__secondary-link:focus-visible,
  .admin-layout__mobile-toggle:focus-visible {
    outline:
      3px solid var(--orange);

    outline-offset:
      2px;
  }

  /* ================================================================
     SCROLL LOCK
  ================================================================= */

  :global(
    html.admin-scroll-locked
  ),
  :global(
    body.admin-scroll-locked
  ) {
    overflow: hidden;
  }

  /* ================================================================
     REDUCED MOTION
  ================================================================= */

  @media (prefers-reduced-motion: reduce) {
    .admin-layout,
    .admin-layout *,
    .admin-layout::before,
    .admin-layout::after {
      scroll-behavior: auto !important;

      transition-duration:
        0.01ms !important;

      animation-duration:
        0.01ms !important;

      animation-iteration-count:
        1 !important;
    }
  }
</style>